.jumbotron
  %h1{ class: 'text-xs-right display-3' } Articles
  %p{ class: 'text-xs-right lead' } Helpful snippets of advice for fellow Rubyists and others.
.container-fluid
  .row#article-tags
    %div{ class: 'col-xs-12 col-md-10 offset-md-1 col-lg-8 offset-lg-2' }
      .card
        .card-block
          %p.card-text a list of tags to come soon
  .row#article-controls
    %div{ class: 'col-xs-12 col-md-6', id: 'new-article' }
      = link_to 'New article...', new_article_path, class: 'btn btn-secondary'
  .row#article-list
    - @articles.each do |article|
      %div{ class: 'col-xs-12 col-md-6' }
        .card
          .card-header tags (coming soon)
          .card-block
            %h2{ class: 'card-title text-xs-center' }= article.title
            %p.card-text= article.text
            %div{ class: 'btn-group btn-group-md', role: 'group' }
              = link_to 'Read', article_path(article), class: 'btn btn-outline-primary'
              = link_to 'Modify', edit_article_path(article), class: 'btn btn-outline-warning'
              = link_to 'Destroy', article_path(article), class: 'btn btn-outline-danger', method: :delete, data: { confirm: 'Are you sure?' }
